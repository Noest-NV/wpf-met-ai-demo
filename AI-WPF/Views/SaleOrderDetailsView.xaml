<UserControl x:Class="AI_WPF.Views.SaleOrderDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Style="{StaticResource H1}">
                <Run Text="Sale order #"/>
                <Run Text="{Binding Order.Id, Mode=OneWay}"/>
            </TextBlock>
            <TextBlock Style="{StaticResource Muted}" Margin="0,0,0,12">
                <Run Text="Status: "/>
                <Run Text="{Binding Order.Status}"/>
            </TextBlock>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Text="Robot" Style="{StaticResource H2}"/>
                <StackPanel Orientation="Horizontal">
                    <Border Background="#F3F4F6" CornerRadius="6" Width="180" Height="140">
                        <Image Source="{Binding Order.PrimaryLine.Robot.PreviewImage}" Stretch="Uniform" Margin="8"/>
                    </Border>
                    <StackPanel Margin="16,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Order.PrimaryLine.Robot.Name}" FontSize="18" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding Order.PrimaryLine.Robot.Description}" TextWrapping="Wrap" Style="{StaticResource Muted}" MaxWidth="420"/>
                        <TextBlock Margin="0,8,0,0" FontSize="16">
                            <Run Text="Price: "/>
                            <Run Text="{Binding Order.PrimaryLine.Price, StringFormat={}{0:C0}}" FontWeight="SemiBold"/>
                        </TextBlock>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Back" Style="{StaticResource SecondaryButton}" Command="{Binding BackCommand}" Margin="0,0,8,0"/>
            <Button Content="Reject" Style="{StaticResource SecondaryButton}" Command="{Binding RejectCommand}" Margin="0,0,8,0"/>
            <Button Content="Approve" Style="{StaticResource PrimaryButton}" Command="{Binding ApproveCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
