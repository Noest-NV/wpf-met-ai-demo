<UserControl x:Class="AI_WPF.Views.CatalogueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Catalogue" Style="{StaticResource H1}" />
            <TextBlock Text="Pick a robot and place your order." Style="{StaticResource Muted}" Margin="0,0,0,12"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Robots}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource Card}" Width="260">
                            <StackPanel>
                                <Border Background="#F3F4F6" CornerRadius="6" Height="140">
                                    <Image Source="{Binding PreviewImage}" Stretch="Uniform" Margin="8" />
                                </Border>
                                <TextBlock Text="{Binding Name}" Style="{StaticResource H2}" Margin="0,12,0,4"/>
                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Style="{StaticResource Muted}" Height="48"/>
                                <StackPanel Orientation="Horizontal" Margin="0,12,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Price, StringFormat={}{0:C0}}" FontSize="18" FontWeight="SemiBold"/>
                                    <Button Content="Buy now"
                                            Style="{StaticResource PrimaryButton}"
                                            Margin="12,0,0,0"
                                            Command="{Binding DataContext.BuyNowCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}" />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
