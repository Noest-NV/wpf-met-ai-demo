<UserControl x:Class="AI_WPF.Views.ProductionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Production" Style="{StaticResource H1}"/>
            <TextBlock Text="Robots currently being built." Style="{StaticResource Muted}" Margin="0,0,0,12"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}">
            <DataGrid ItemsSource="{Binding Orders}"
                      SelectedItem="{Binding SelectedOrder}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      BorderThickness="0"
                      Background="Transparent"
                      RowBackground="Transparent">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Order #" Binding="{Binding Id}" Width="80"/>
                    <DataGridTextColumn Header="Robot" Binding="{Binding Robot.Name}" Width="*"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
                    <DataGridTextColumn Header="Estimated completion" Binding="{Binding EstimatedCompletion, StringFormat={}{0:d}}" Width="180"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="View details" Style="{StaticResource PrimaryButton}" Command="{Binding ViewDetailsCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
