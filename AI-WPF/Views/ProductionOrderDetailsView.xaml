<UserControl x:Class="AI_WPF.Views.ProductionOrderDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Style="{StaticResource H1}">
                <Run Text="Production order #"/>
                <Run Text="{Binding Order.Id, Mode=OneWay}"/>
            </TextBlock>
            <TextBlock Style="{StaticResource Muted}" Margin="0,0,0,12">
                <Run Text="Robot: "/>
                <Run Text="{Binding Order.Robot.Name}"/>
                <Run Text="   |   Status: "/>
                <Run Text="{Binding Order.Status}"/>
                <Run Text="   |   ETA: "/>
                <Run Text="{Binding Order.EstimatedCompletion, StringFormat={}{0:d}}"/>
            </TextBlock>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Text="Components" Style="{StaticResource H2}"/>
                <DataGrid ItemsSource="{Binding Order.Lines}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          BorderThickness="0"
                          Background="Transparent"
                          RowBackground="Transparent">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Component" Binding="{Binding ComponentName}" Width="*"/>
                        <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Back" Style="{StaticResource SecondaryButton}" Command="{Binding BackCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
